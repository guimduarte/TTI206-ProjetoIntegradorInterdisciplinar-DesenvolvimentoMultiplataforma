import 'package:flutter/material.dart';

class MostRecentsPage extends StatelessWidget {
  const MostRecentsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Text('página de mais recentes');
  }
}